[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "deq-zoo"
version = "0.0.1"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["."]
include = ["deq_lm", "deq_lm.*"]

[tool.setuptools.package-dir]
deq_lm = "deq-lm"
